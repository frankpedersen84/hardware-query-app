version: "1"
services:
  - type: web
    name: hardware-query-app
    runtime: python
    repo: https://github.com/frankpedersen84/hardware-query-app
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt && chmod +x prestart.sh && chmod +x startup.sh
    startCommand: bash -c 'chmod +x prestart.sh && chmod +x startup.sh && ./prestart.sh && exec gunicorn wsgi:app'
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
      - key: OPENAI_API_KEY
        sync: false
      - key: RENDER
        value: "true"
      - key: PORT
        value: "10000"
